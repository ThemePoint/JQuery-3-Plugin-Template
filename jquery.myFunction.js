(function($){
    $.fn.extend({
        myFunction: function (options) {
            options = $.extend({}, options);

            // Code runs here...

            return this;
        }
    });
})(jQuery);
